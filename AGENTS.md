# Repository instructions

- Do not preserve backward compatibility, legacy configuration syntax, or deprecated APIs unless the user explicitly requests it. Prefer the requested breaking change over compatibility shims.
